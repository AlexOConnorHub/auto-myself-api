info:
  name: Create File for Maintenance Record (1)
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_url}}/v1/file/maintenance/:maintenance_id"
  params:
    - name: maintenance_id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
