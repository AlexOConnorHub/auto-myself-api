info:
  name: List Files for Maintenance
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/file/maintenance"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
