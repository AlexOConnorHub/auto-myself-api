info:
  name: Update maintenance
  type: http
  seq: 3

http:
  method: PATCH
  url: "{{base_url}}/v1/maintenance/:maintenance_id"
  params:
    - name: maintenance_id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
