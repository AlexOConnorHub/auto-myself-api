info:
  name: Accept Share
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{base_url}}/v1/share/:share_id"
  params:
    - name: share_id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
