info:
  name: Delete share
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{base_url}}/v1/share/:share_id"
  params:
    - name: share_id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
