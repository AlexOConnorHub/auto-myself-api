info:
  name: Delete vehicle
  type: http
  seq: 6

http:
  method: GET
  url: "{{base_url}}/v1/vehicle/:vehicle_id"
  params:
    - name: vehicle_id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
