info:
  name: Update vehicle
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{base_url}}/v1/vehicle/:vehicle_id"
  params:
    - name: vehicle_id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
