info:
  name: User 5
  type: http
  seq: 5

http:
  method: POST
  url: "{{base_url}}/auth/development"
  body:
    type: json
    data: |
      {
        "user_id": "{{user_5}}"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
