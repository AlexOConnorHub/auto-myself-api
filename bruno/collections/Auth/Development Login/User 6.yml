info:
  name: User 6
  type: http
  seq: 6

http:
  method: POST
  url: "{{base_url}}/auth/development"
  body:
    type: json
    data: |
      {
        "user_id": "{{user_6}}"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
