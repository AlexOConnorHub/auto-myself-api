info:
  name: User 8
  type: http
  seq: 8

http:
  method: POST
  url: "{{base_url}}/auth/development"
  body:
    type: json
    data: |
      {
        "user_id": "{{user_8}}"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
